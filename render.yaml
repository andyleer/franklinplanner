services:
  - type: web
    name: franklin-planner
    env: python
    buildCommand: ""
    startCommand: "gunicorn app:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: planner-db
          property: connectionString
